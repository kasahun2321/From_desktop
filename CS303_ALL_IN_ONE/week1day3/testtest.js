/*eslint-disable*/
"use strict"

describe("comparing three number",()=>
{
    it("let sort 3,10 and 1",()=>
    {
        // assert.equal(sort(3,10,1),1);
        // assert.equal(sort(3,10,1),3);
        // assert.equal(sort(3,10,1),10);
       assert(true);
    });
    it ("check if it not a number",()=>
    {
        assert.isNaN(sort(3,10,1),false)
    })


});