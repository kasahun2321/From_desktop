const arr = [1,2,-1,3,-2,5,6] 
const number_above_zero = arr.filter(n=> n >= 0);
console.log(number_above_zero);
function filter_positive_number(arr)
let j=0;
let temp=[];
for (let i=0; i<=arr.length;i++)
{
    if (arr[i]-1>=0)
    {
        console.log(arr[i]+)
        j=j+1;
    }
}